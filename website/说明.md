# website —— 网站代码

## 本文件夹内容

整个网站的代码，是一个独立的 Vite 项目：

- `index.html`：第 1 层首页（游戏大厅）
- `package.json`、`vite.config.js`：项目依赖与构建配置
- `src/`：源代码（全局样式、可复用部件、页面逻辑、游戏、公共工具）
- `tests/`：自动化测试

## 目录约定

每个子文件夹都包含 `说明.md`（内容声明）和 `修改日记.md`（修改记录）。

## 使用方法

代码实现完成后在此补充：安装依赖、本地预览、打包发布的命令。
