# 项目计划（plan.md）

本文件是项目唯一的总文档，汇总项目层级、文件结构管理、网站设计与细节记录。每当确定一个项目细节，都在本文件登记，并注明所属的**层级**和**部分**。

## 1. 项目层级

| 层级 | 名称 | 想要实现的效果 |
|---|---|---|
| 第 1 层 | 主页面 | 选择不同的小游戏、登录个人账户等都在这里完成 |
| 第 2 层 | 游戏详情页 | 展示某一小游戏的详细信息，可选择难度等 |
| 第 3 层 | 游戏页 | 开始游戏 |

## 2. 文件结构管理

### 约定

- program1 根目录只允许新增**文件夹**，不允许新增文件。
- 每个文件夹包含 `说明.md`（声明该文件夹内容）与 `修改日记.md`（记录该文件夹内容修改）。
- 修改任何内容时，在对应文件夹的 `修改日记.md` 中追加记录。

### 目录结构

```
program1/
├─ website/          网站代码（独立 Vite 项目）
│  ├─ index.html     第 1 层首页
│  ├─ package.json
│  ├─ vite.config.js
│  ├─ src/
│  │  ├─ styles/     全局样式（base / glass / home）
│  │  ├─ components/ 可复用部件（game-card / auth-entry）
│  │  ├─ pages/      页面逻辑（home.js）
│  │  ├─ games/      小游戏（snake/：index 详情页、play 游戏页、core 核心逻辑）
│  │  └─ shared/     公共工具（navigation / storage）
│  └─ tests/         自动化测试
└─ docs/             项目文档
```

### 新增游戏流程

1. 在 `src/games/` 下新建游戏文件夹（含 `说明.md` 与 `修改日记.md`）。
2. 参照 `snake/` 的结构写页面、逻辑、样式。
3. 在首页加一张游戏卡片。

## 3. 网站设计

### 三层结构与跳转

- 首页 `index.html` → 点击游戏卡片 → `src/games/snake/index.html`（详情页）
- 详情页选择难度后点“开始游戏” → `src/games/snake/play.html`（游戏页）
- 游戏页可返回详情页或首页

### 首页设计

- **背景**：玻璃拟态——深色渐变 + 彩色线条（青/紫/粉）流动动画 + 光晕光影，纯 CSS 实现，不使用图片。
- **卡片**：半透明毛玻璃卡片，鼠标划过时跟随鼠标轻微倾斜与浮动。
- **内容**：网站标题、游戏卡片（名称 + 一句话简介，不放“开始游戏”按钮）、右上角登录入口（v0.1.0 仅样式占位）。

### 技术方案

- Vite + 原生 HTML/CSS/JS（不引入前端框架）。
- 多页面结构：`index.html` / `src/games/snake/index.html` / `src/games/snake/play.html`。
- 贪吃蛇核心逻辑独立为 `core.js`，不依赖 DOM，可单独测试。

### 贪吃蛇功能清单

- Canvas 渲染、方向键控制、计分、最高分（本地记录）。
- 难度：简单 / 普通 / 困难（对应不同速度）。
- 暂停、重开、结束判定、返回。

### 测试与验证

- `tests/snake-core.test.js`：蛇的移动、碰撞、吃食物逻辑。
- 完成后本地构建并在浏览器实际预览验证。

### 版本管理

- 按 `git.md` 规范：feat 分支开发、提交规范、合并 main、里程碑打标签。
- v0.1.0：网站骨架 + 首页 + 贪吃蛇。
- v0.2.0：登录功能（`src/pages/auth/`）。
- 后续：在 `src/games/` 下新增更多游戏文件夹。

## 4. 当前计划（2026-08-12）

- 第 1 层（主页面）：选择不同的小游戏、登录个人账户
- 第 2 层（游戏详情页）：游戏详细信息、难度选择
- 第 3 层（游戏页）：开始游戏
- 当前进度：目录骨架已搭建，待实现网站代码

## 5. 细节记录

每确定一个具体细节，按「层级 + 部分」登记到下表。

| 日期 | 层级 | 部分 | 确定的细节 |
|---|---|---|---|
| 2026-08-12 | 整体 | 游戏范围 | 第一款小游戏确定为贪吃蛇 |
| 2026-08-12 | 整体 | 文档整合 | 文件结构管理与网站设计内容整合进本文件，不再单独保留网站设计文档 |

## 6. 本文件修改记录

| 日期 | 修改内容 |
|---|---|
| 2026-08-12 | 创建本文件，登记三层结构与贪吃蛇计划 |
| 2026-08-12 | 整合文件结构管理与网站设计内容，替代网站设计文档 |
