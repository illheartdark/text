# Git 版本管理方案

> **功能：** 项目的版本管理手册 + 代码更新记录。规定提交信息、分支、版本标签等规范，并登记每次代码更新，确保任何改动都可回溯、可查、可维护。

## 1. 总体思路

用 Git 的三件套实现「时间树」：

- **提交（commit）**：每一次代码更新 = 时间树上的一个节点，记录"改了什么、为什么改、什么时候改、谁改的"。
- **分支（branch）**：一条条并行的时间线。主线永远保持稳定可用，新功能在支线上开发完再合并回主线。
- **标签（tag）**：给重要的时间节点（比如某个版本完成）打个标记，以后随时能跳回那一刻。

## 2. 分支规则

- `main`：主线。永远保持"能运行、没坏功能"的状态。
- 开发新功能时新建分支：`feat/功能名`（例如 `feat/snake`），完成并验证后再合并回 `main`。
- 修复问题：`fix/问题名`（例如 `fix/snake-collision`）。

### 常用分支操作

```bash
# 从主线开新分支
git checkout -b feat/snake

# 开发完成后切回主线并合并
git checkout main
git merge feat/snake

# 合并后删除分支
git branch -d feat/snake
```

## 3. 提交信息规范

格式：`类型: 中文描述`

| 类型 | 含义 | 示例 |
|---|---|---|
| feat | 新功能 | feat: 完成贪吃蛇移动逻辑 |
| fix | 修复 bug | fix: 修复贪吃蛇穿墙问题 |
| docs | 文档 | docs: 更新 git.md 方案说明 |
| refactor | 重构（不改功能） | refactor: 拆分游戏逻辑文件 |
| test | 测试 | test: 补充登录模块测试 |
| style | 格式调整 | style: 统一页面间距 |
| chore | 其他维护 | chore: 更新依赖版本 |

每条提交只做一件事，描述用一句话说清楚"做了什么"。

```bash
git add 具体文件
git commit -m "feat: 完成贪吃蛇基础游戏逻辑"
```

## 4. 里程碑标签

完成一个重要阶段后打标签，版本号规则：`v主版本.次版本.修订号`

| 版本 | 含义 |
|---|---|
| v0.1.0 | 第一个可玩版本（网站框架 + 贪吃蛇） |
| v0.2.0 | 加入登录功能 |
| v1.0.0 | 正式发布版 |

```bash
git tag -a v0.1.0 -m "第一个可玩版本：框架+贪吃蛇"
```

## 5. 更新流程（每次开发都按这个顺序）

1. 从 `main` 开新分支（或确认在正确分支上）
2. 写代码
3. 按规范提交
4. 合并回 `main`
5. 如果是里程碑，打标签
6. 在下方"更新记录"表中登记一行
7. 提交更新记录

## 6. 查看时间树

```bash
# 树状查看完整历史
git log --oneline --graph --all

# 只看最新 10 条
git log --oneline -10

# 查看某次提交改了什么
git show <提交号>

# 对比两个版本之间的差异
git diff v0.1.0 v0.2.0
```

## 7. 回溯到某个版本

```bash
# 临时查看历史版本的某个文件
git show <提交号>:路径/文件名

# 把某次提交中的某个文件恢复到现在
git checkout <提交号> -- 路径/文件名

# 撤销某次提交（谨慎使用，撤销前先确认）
git revert <提交号>
```

> 回退操作有风险，动手前先确认，必要时先把当前状态打好标签备份。

## 8. 更新记录

| 日期 | 版本 | 提交摘要 | 更新内容 | 说明 |
|---|---|---|---|---|
| 2026-08-12 | 初始 | 初始化项目 | 建立 git 仓库、编写 git.md 版本管理方案 | 项目起点，代码基线 |
| 2026-08-12 | 初始 | 纳入行为日志 | 将 log.md 行为日志纳入版本管理并登记首次任务 | log.md 与 git.md 配套使用 |
| 2026-08-12 | 初始 | 关联 GitHub | 关联远程仓库 text.git 并推送 main 分支 | 本地与云端同步 |
| 2026-08-12 | 初始 | 移除个人配置 | 将 agent.md、skill.md 移出仓库并加入 .gitignore | 仓库只保留项目相关文件 |
| 2026-08-12 | 初始 | 功能说明 | 明确 git.md 的功能：版本管理手册与代码更新记录 | 文件描述与用途一致 |
| 2026-08-12 | 初始 | 项目计划 | 新建 plan.md 记录项目层级效果与细节 | 后续细节按层级、部分登记 |
