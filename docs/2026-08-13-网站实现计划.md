# 网站实现计划（v0.1.0）
**功能：** 网站代码实现计划
**编号：** 26

> 日期：2026-08-13
> 目标：完成三层小游戏网站 v0.1.0——首页（游戏大厅）＋贪吃蛇详情页＋贪吃蛇游戏页，本地可直接打开游玩。

## 技术方案（已确认调整）

- 纯静态 HTML/CSS/JS，零依赖、无需安装任何包。
- 说明：原方案为 Vite＋原生 JS。鉴于当前网络受限（无法安装依赖）且项目从简优先，v0.1.0 改为纯静态实现；目录结构与代码职责不变，后续如需打包再引入 Vite。
- 页面使用普通 `<script>` 引入（非 ES 模块），双击 index.html 即可打开。
- 贪吃蛇核心逻辑 `core.js` 采用通用格式（同时兼容浏览器与 Node 测试）。

## 全局约束

- program1 根目录只允许新增文件夹，不允许新增文件。
- 每个文件夹包含 `说明.md` 与 `修改日记.md`，只关联本文件夹内容。
- 新建 .md 文件须分配编号（见 agent.md 对照表）并写入「编号」行。
- 核心逻辑采用 TDD：先写测试，确认失败，再实现，确认通过。
- 每个任务完成提交一次，提交信息遵循 git.md 规范。

## 文件地图

- 创建 `website/index.html`：第 1 层首页（游戏大厅）
- 创建 `website/src/styles/base.css`、`glass.css`、`home.css`：全局样式
- 创建 `website/src/components/game-card.js`、`auth-entry.js`：可复用部件
- 创建 `website/src/pages/home.js`：首页逻辑
- 创建 `website/src/games/snake/index.html`、`play.html`、`detail.js`、`play.js`、`core.js`、`style.css`：贪吃蛇三层
- 创建 `website/src/shared/navigation.js`、`storage.js`：公共工具
- 创建 `website/tests/snake-core.test.js`：核心逻辑测试
- 修改各文件夹 `说明.md` 与 `修改日记.md`

## 接口约定

- `SnakeCore.createGame({ cols, rows, difficulty })` → `{ getState, step, reset }`
  - `state`：`{ snake, direction, food, score, status, speed }`
  - `step()`：前进一格，返回 `{ ate, over }`
  - 方向键取值：`up / down / left / right`；禁止直接反向
  - `difficulty`：`easy / normal / hard`（决定初始速度）
- `storage.getBest(key)` / `storage.setBest(key, score)`：localStorage 封装
- `navigation.go(path)`：页面跳转封装

## 任务清单

### 任务 1：贪吃蛇核心逻辑（TDD）

- 写 `tests/snake-core.test.js`：初始状态、前进、转向（禁止反向）、吃到食物增长加分、撞墙结束、撞自身结束、难度对应速度。
- 运行 `node --test` 确认测试失败。
- 实现 `core.js`，运行确认全部通过。

### 任务 2：公共工具

- `storage.js`：最高分读写。
- `navigation.js`：返回 / 跳转。

### 任务 3：全局样式与首页

- `base.css`：变量、重置、字体。
- `glass.css`：玻璃背景（彩色线条＋光影＋毛玻璃卡片）。
- `home.css`：首页布局。
- `game-card.js`：鼠标跟随倾斜动效。
- `auth-entry.js`：登录入口占位。
- `pages/home.js`：渲染卡片、跳转。
- `index.html` 组装。

### 任务 4：贪吃蛇详情页

- `snake/index.html` ＋ `detail.js`：玩法介绍、难度选择、开始游戏按钮（跳转 `play.html?difficulty=...`）。

### 任务 5：贪吃蛇游戏页

- `snake/play.html` ＋ `play.js` ＋ `style.css`：Canvas 渲染、键盘控制、计分、最高分、暂停 / 重开 / 返回。

### 任务 6：文档同步与收尾

- 更新各文件夹 `说明.md` / `修改日记.md`。
- 本地静态服务验证三个页面可访问。
- 按 git.md 合并 main、打标签 v0.1.0、登记更新记录。
