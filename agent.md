# agent.md — 工作行为规则

本文件是强制执行的行为规则。**每次执行任务前必须先阅读本文件**，文件中要求的行为必须**严格遵守**，不得跳过、简化或变通。

## 规则

### 规则 1：计划任务前先查阅 skill.md

每次计划任务时，必须先打开 `skill.md` 文件，查找其中是否有能够帮得上忙的 Skill；若找到适用的 Skill，必须按该 Skill 的指导执行，不得绕过。

了解各 Skill 的功能只需阅读 `skill.md` 这一个文件即可，**无需读取各 Skill 的原 SKILL.md 文件**。

### 规则 2：执行任务前先阅读本文件

每次执行任务前，必须先阅读 `agent.md`（本文件），确认其中所有规则后再开始工作。

### 规则 3：严格遵守本文件要求

本文件要求的行为必须严格遵守；不得自行跳过、简化或变通其中任何规则。如与用户本次的明确指示冲突，以用户指示为准，但需在开始前说明。

### 规则 4：开始执行任务时立即记录行为日志

每次开始执行任务时，必须**立即**在 `log.md` 文件中写入本次任务的内容和开始时间，之后再进行实际操作。

### 规则 5：执行代码更新时立即记录更新日志

每次执行代码更新时，必须**立即**在 `git.md` 文件中写入本次更新的内容和时间，之后再进行实际操作。
